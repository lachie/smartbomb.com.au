.entrytitle
  %h2
    %a{:href=>post.url}=post.title
  %p.postdate=post.published_at.strftime('<em>%d</em><br/>%b')

.entrybody
  =post_html(post)

.entrymeta
  posted by
  %strong Lachie
  -#-unless post.tags.blank?
  -#  |
  -#  %strong tags
  -#  \::
  -#  =linked_tag_list(post.tags)

  -# = link_to(pluralize(post.approved_comments.size, "archived comment")+", ", post_path(post, :anchor => 'comments'))
  -if post.archived_comments != '0'
    %a{:href => "http://smartbomb.com.au#{post.url}"}
      = post.archived_comments
      archived comments

  %a{:href => "http://smartbomb.com.au#{post.url}#disqus_thread"} Comments
